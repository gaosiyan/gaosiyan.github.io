---
sidebar_position: 3
sidebar_label: "自定义组件"
---
import remarkGfm from 'remark-gfm'
import Figure, { FigureRef } from "@site/src/components/custom/Figure";
import Table, { TableRef } from "@site/src/components/custom/Table";

# Figure 自定义组件

全量接口使用:

```jsx
<Figure
  src="/img/content/3d88ea8fe3a24e4d8c8327629f9ec2bcec6e6406.png" {/* 图片路径,必选参数 */}
  title="普通标题" {/* 图片标题,必选参数, 支持 LaTeX 数学公式 */}
  id="图片测试 1"  {/* 图片锚点,可选参数, 默认值和 title 相同 */}
  width="80%"     {/* 图片宽度,可选参数, 默认值 80% */}
/>
```

对应的锚点使用:

```jsx
如 <FigureRef id="图片测试 1" /> 所示...
```

渲染效果如下 <FigureRef id="图片测试 1" /> 所示:

<Figure
  src="/img/content/3d88ea8fe3a24e4d8c8327629f9ec2bcec6e6406.png"
  title="普通标题"
  id="图片测试 1"
  width="80%"
/>

在图片后面引用锚点,如上 <FigureRef id="图片测试 1" /> 所示...

最简化的接口调用:

只用 `src` 和 `title` 两个必选参数,锚点依然可用,可以使用 `title` 作为锚点使用:

```jsx
<Figure
  src="/img/content/3d88ea8fe3a24e4d8c8327629f9ec2bcec6e6406.png"
  title="最简化的接口调用"
/>
```

渲染效果如下 <FigureRef id="最简化的接口调用" /> 所示:

<Figure
  src="/img/content/3d88ea8fe3a24e4d8c8327629f9ec2bcec6e6406.png"
  title="最简化的接口调用"
/>

标题中包含 LaTeX 数学公式(支持标题任意位置包含 LaTeX 数学公式):

```jsx
<Figure
  src="/img/content/3d88ea8fe3a24e4d8c8327629f9ec2bcec6e6406.png"
  title="$x^2$"
  id="标题中只有_LaTeX_数学公式"
/>
```

渲染效果如下 <FigureRef id="标题中只有 LaTeX 数学公式" /> 所示:

<Figure
  src="/img/content/3d88ea8fe3a24e4d8c8327629f9ec2bcec6e6406.png"
  title="$x^2$"
  id="标题中只有 LaTeX 数学公式"
/>

| <div style={{ textAlign: 'right' }}>表头 1</div>     | 表头 2            |
| ---------- | ----------------- |
| $E = mc^2$ | $a^2 + b^2 = c^2$ |

<Table title="表格标题" id="table1">   
| <div style={{ textAlign: 'right' }}>表头 1</div>     | 表头 2            |
| ---------- | ----------------- |
| $E = mc^2$ | $a^2 + b^2 = c^2$ |
</Table>

如 <TableRef id="table1" /> 所示:
如 <TableRef id="table2" /> 所示:

<Table title="支持公式的合并表格" id="working-table">
  <table>
    <thead>
      <tr>
        {/* 注意:JSX 中合并属性用 colSpan/rowSpan(驼峰式) */}
        <th colSpan={2}>表头跨 2 列($x^2 + y^2 = r^2$)</th>
        <th>表头 3</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td rowSpan={2}>
          左列跨 2 行<br />
          公式:$E = mc^2$
        </td>
        <td>
          单元格 (2,2)<br />
          $a + b = c$
        </td>
        <td>
          单元格 (2,3)<br />
          $\sum_{i=1}^n i$
        </td>
      </tr>
      <tr>
        <td>
          单元格 (3,2)<br />
          $\int_a^b f(x)dx$
        </td>
        <td>
          单元格 (3,3)<br />
          $\lim_{x \to 0} \frac{\sin x}{x}$
        </td>
      </tr>
      <tr>
        <td colSpan={3}>
          底部跨 3 列($\alpha + \beta = \gamma$)
        </td>
      </tr>
    </tbody>
  </table>
</Table>
